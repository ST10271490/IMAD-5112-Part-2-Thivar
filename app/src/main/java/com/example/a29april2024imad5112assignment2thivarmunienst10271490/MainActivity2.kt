package com.example.a29april2024imad5112assignment2thivarmunienst10271490

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import androidx.activity.enableEdgeToEdge

class MainActivity2 : AppCompatActivity() {
    //declare


    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContentView(R.layout.activity_main2)

        //initialise

        Playbar = findViewById(R.id.Playbar)
        // increase the progressbar by 50
        button.setOnClickListener {
            fitView!
            imageview.setImageResource(R.drawable.2 dags playing)
            Feedbar.incrementProgressBy(diff 50)
        }
        {
        button2.setOnClickListener fit View!
        imageView.setImageResource(R.drawable.dog eating)
        Cleanbar.incrementProgressBy(diff 50)
    }
}
    button3.setOnClickListener
    {
        itView!
        inageView.setImageResource(R.drawable.clean dog new)
        PLaybar.incrementProgressBy(diff 50)
    }



















    }
}